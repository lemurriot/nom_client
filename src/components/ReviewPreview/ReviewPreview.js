import React from 'react'

export default function ReviewPreview(props) {
    return (
        <section className="category-card">
            <h2>{props.title}</h2>
            ReviewPreview
        </section>
    )
}
